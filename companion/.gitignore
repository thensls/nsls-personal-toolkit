.venv/
__pycache__/
*.pyc
.pytest_cache/
*.egg-info/

# Private CPython runtime provisioned by ensure-companion.sh on machines with
# no Python >=3.10 — per-machine, never committed. The wildcard also covers
# its $$-suffixed staging directories and partial downloads.
.python-runtime*
