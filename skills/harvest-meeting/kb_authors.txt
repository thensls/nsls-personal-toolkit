# SLT members authorized to write to thensls/nsls-knowledge.
#
# ⚠ NOT the source of truth, and DELIBERATELY EMPTY.
#
# The live allowlist lives in the KB repo at _data/kb_authors.txt
# (thensls/nsls-knowledge, private); Step 0 reads it from origin/main on every
# run. This file is only the offline / first-run (pre-clone) fallback.
#
# WHY IT IS EMPTY: this repository is PUBLIC. A populated fallback here is a
# roster — it publishes who is on the SLT, by work address, to anyone who
# clones the toolkit. The membership list is exactly the sensitive part, and
# it does not need to be here to work.
#
# WHAT AN EMPTY FALLBACK COSTS: nothing that matters. Step 0 only reaches this
# file when the KB repo has not been cloned yet. With no entries it routes the
# run to the builder's LOCAL KB and prints the "NSLS email detected but NOT in
# KB_AUTHORS" guidance — which is accurate, because a writer without the clone
# could not have written to the company KB anyway (Step 1a exits if the company
# KB is missing). Once references/setup.sh clones the repo, the live allowlist
# takes over and routing is correct from then on.
#
# DO NOT re-populate this file. To add a member, commit to _data/kb_authors.txt
# in the KB repo. Every writer picks it up on their next harvest.
#
# The file must continue to EXIST: Step 0 exits 2 if it is missing entirely.
#
# Format, if it were ever used: one email per line, # comments, blanks ignored.
